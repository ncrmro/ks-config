# Archived skills, 2026-06-09

This archive holds the agent skill tree removed from the live agent surfaces
while the skills are reevaluated.

## Contents

- `skills/` contains the previous canonical `.agents/skills` skill directories.
- `claude/skills/` contains the previous Claude-facing symlinks.
- `_shared/conventions/` preserves the convention files targeted by archived
  skill symlinks.

## Live surfaces cleared

- `agents/skills/`
- `agents/claude/skills/`
- Bridl profile `controls.skills` entries in `platform-engineer`,
  `project-manager`, `product-lead`, and `vega`
