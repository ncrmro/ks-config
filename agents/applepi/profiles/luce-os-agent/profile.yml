id: luce-os-agent
label: Luce — OS agent launch (persona + fleet + agent-user env)
# Composite launch profile: applepi `run --profile` takes a single profile,
# so the persona/fleet/environment layers compose here instead of via
# repeated -p flags.
inherits:
  - luce
  - ks-fleet
  - os-ks-agent-user
