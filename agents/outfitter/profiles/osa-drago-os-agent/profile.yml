id: drago-os-agent
label: Drago — OS agent launch (persona + fleet + agent-user env)
# Composite launch profile: outfitter `run --profile` takes a single profile,
# so the persona/fleet/environment layers compose here instead of via
# repeated -p flags.
inherits:
  - drago
  - ks-fleet
  - os-ks-agent-user
