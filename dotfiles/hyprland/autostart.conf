# Session autostart.
# D-Bus activation environment — required for app notifications to reach mako.
exec-once = systemctl --user import-environment
exec-once = dbus-update-activation-environment --systemd --all

# Session starts locked; unlock with the account password.
exec-once = hyprlock

exec-once = waybar
exec-once = mako
# hyprpaper stays off until the stow tree carries a backgrounds/ dir
# (theming follow-up) — without a config it only logs errors.
# exec-once = hyprpaper
exec-once = hypridle
exec-once = systemctl --user start hyprpolkitagent

# Clipboard: persist after apps close, and record history for clipse.
exec-once = wl-clip-persist --clipboard regular & clipse -listen
